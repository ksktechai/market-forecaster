# Runtime dependencies for the HTTP service + pipeline.
# NOTE: torch + timesfm are intentionally NOT here -- they are heavy and
# installed separately (see requirements-model.txt / Dockerfile) so that the
# test suite and the HTTP layer can be installed/run without them.
fastapi==0.115.6
uvicorn[standard]==0.34.0
requests==2.32.3
pydantic==2.10.4
APScheduler==3.11.0
numpy==2.2.1
python-dotenv==1.0.1
# Keyless daily price history (Yahoo). A recent yfinance is required -- older
# versions hit a now-dead Yahoo endpoint and fail with "Expecting value".
# curl_cffi impersonates a browser so Yahoo doesn't 429/anti-bot block us.
yfinance==1.5.1
curl_cffi==0.15.0
